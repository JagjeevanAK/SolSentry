{
    "name": "solsentry",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "packageManager": "bun@1.0.0",
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "lint": "turbo run lint",
        "test": "turbo run test"
    },
    "devDependencies": {
        "turbo": "^2.3.3"
    }
}